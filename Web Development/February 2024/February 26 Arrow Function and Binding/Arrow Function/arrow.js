let fun = (name, age)=>{
    console.log(name, age);
}

fun("Manav", 24);

let sum = (a, b)=>{ //User returns a value
    return a + b;
}

console.log(sum(10, 20));

let sub = (a, b)=>a-b; //Default return
console.log(sub(20, 10));